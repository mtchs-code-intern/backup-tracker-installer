[package]
name = "backup-tracker-installer"
version = "0.1.0"
edition = "2024"

[dependencies]
winreg = "0.52"
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }